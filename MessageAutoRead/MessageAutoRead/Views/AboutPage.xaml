<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MessageAutoRead.Views.AboutPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
  
    xmlns:vm="clr-namespace:MessageAutoRead.ViewModels"
    Title="{Binding Title}">

    <ContentPage.Content>
        <StackLayout Padding="10,0,10,0" VerticalOptions="Center">
           
            <Button Clicked="ImageButton_OnClicked" Text="Wait for sms" />
            <Label x:Name="TimerLabel" HorizontalTextAlignment="Center" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
